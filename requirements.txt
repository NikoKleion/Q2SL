# core
numpy>=1.24

# optional extras (install what you need, or use: pip install -e ".[full]")
# scipy          exact Wasserstein leak distance
# pyzx           ZX gate identity
# qiskit         device T1, T2 and gate durations, and the hardware measurement (with qiskit-ibm-runtime)
# torch, ldpc    the full reconstruction
# matplotlib     demo plots
# pytest         tests
